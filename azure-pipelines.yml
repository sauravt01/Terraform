pool: Default 
trigger: none 
variables: 
  name: Saurav tripathi
  father_name: xxx
parameters:
- name: environment
  type: string
  values:
  - dev
  - qa
  - prod
- name: which_server
  type: string
  values:
  - Appache
  - nginx
  - IIS



steps: 
- script: |
   echo "Hello my name is $(name) and father name is $(father_name)"
   echo "Deploying to ${{ parameters.environment }} and on ${{ parameters.which_server }} server "
  displayName: "Saurav ka Pipeline"

















